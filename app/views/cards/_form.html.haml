= simple_form_for([card.deck, card], remote: true) do |f|
  .form-inputs
    = f.input :front
  .form-inputs
    = f.input :back
  .form-inputs
    = f.select :word_class, Card.word_classes
  .form-actions
    = f.button :submit, class: 'btn btn-primary btn-sm'
