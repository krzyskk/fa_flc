= content_for :header do
  Decks

%table.table
  %thead
    %tr
      %th Name
      %th{colspan: "2"} Number of cards
  %tbody
    = render(@decks)

= link_to 'New Deck', new_deck_path, class: 'btn btn-primary'
