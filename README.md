# README

This is application for application for learning foreign language.

### Setup:

- Run `bundle install` within your app directory
- Run `rake db:setup` to setup database, it will also fill database with prepared seeds


